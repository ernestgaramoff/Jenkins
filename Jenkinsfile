#!groovy

pipeline {
    agent any
        stage('clone') {
            steps {
                script {
                    echo "Trying to connect to Github repo"
                }
            }
        }
}
